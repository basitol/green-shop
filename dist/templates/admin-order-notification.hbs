<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .container {
            background-color: #f9f9f9;
            border-radius: 5px;
            padding: 20px;
            margin-top: 20px;
        }
        .order-details {
            margin: 20px 0;
            padding: 15px;
            background-color: #fff;
            border-radius: 5px;
            border: 1px solid #ddd;
        }
        .customer-info {
            margin: 15px 0;
            padding: 10px;
            background-color: #f5f5f5;
            border-radius: 5px;
        }
        .product-item {
            padding: 10px 0;
            border-bottom: 1px solid #eee;
        }
        .total {
            margin-top: 15px;
            font-weight: bold;
        }
        .action-required {
            color: #f44336;
            font-weight: bold;
        }
        .footer {
            margin-top: 20px;
            font-size: 12px;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>New Order Notification</h2>
        <p class="action-required">Action Required: New order received</p>
        
        <div class="order-details">
            <h3>Order Information</h3>
            <p>Order Number: {{orderNumber}}</p>
            <p>Order Date: {{orderDate}}</p>
            <p>Order Status: {{status}}</p>
            
            <div class="customer-info">
                <h4>Customer Information</h4>
                <p>Name: {{customerName}}</p>
                <p>Email: {{customerEmail}}</p>
                <p>Phone: {{customerPhone}}</p>
            </div>
            
            <h4>Items Ordered:</h4>
            {{#each items}}
            <div class="product-item">
                <p>{{name}} (SKU: {{sku}})</p>
                <p>Quantity: {{quantity}}</p>
                <p>Price: ${{price}}</p>
            </div>
            {{/each}}
            
            <div class="total">
                <p>Subtotal: ${{subtotal}}</p>
                <p>Shipping: ${{shipping}}</p>
                <p>Total: ${{total}}</p>
            </div>
            
            <div class="shipping-info">
                <h4>Shipping Address</h4>
                <p>{{shippingAddress}}</p>
            </div>
            
            {{#if specialInstructions}}
            <div class="special-instructions">
                <h4>Special Instructions</h4>
                <p>{{specialInstructions}}</p>
            </div>
            {{/if}}
        </div>
        
        <p>Please process this order according to our standard procedures.</p>
        
        <div class="footer">
            <p>This is an automated notification from {{siteName}}.</p>
            <p>For internal use only.</p>
        </div>
    </div>
</body>
</html>
